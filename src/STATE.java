/**
 * Game state, used to display the correct information
 * and respond correctly to input (key presses and clicks)
 */
public enum STATE {
	Game,
	GameMulti,
	Menu,
	MenuSelect,
	Options,
	LevelComplete,
	Help;
}
